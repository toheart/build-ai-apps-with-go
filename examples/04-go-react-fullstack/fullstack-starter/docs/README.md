# 项目约定

这个目录用来放第 4 章最值得长期复用的工程规则。

当前保留的内容有：

- `go-style.md`：Go 代码风格约定
- `typescript-style.md`：前端 TypeScript 风格约定
- `api-conventions.md`：接口路径、响应格式、错误码约定
- `testing.md`：测试层面的基本规则

这一章不要求读者一次把这些文档全部背下来。
更重要的是先建立一个习惯：

当项目从“能跑”进入“多人协作、持续迭代”时，规则应该被写下来，而不是只存在脑子里。
